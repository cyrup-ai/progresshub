// Note: These functions were private in the original module
// Tests are commented out until functions are made public or moved to integration tests

/*
#[test]
fn test_progress_bar_creation() {
    let bar_0 = create_unicode_progress_bar(0);
    let bar_50 = create_unicode_progress_bar(50);
    let bar_100 = create_unicode_progress_bar(100);

    assert!(bar_0.contains("0%"));
    assert!(bar_50.contains("50%"));
    assert!(bar_100.contains("100%"));
}

#[test]
fn test_status_icons() {
    assert_eq!(get_status_icon_optimized(FileStatus::Completed), "󰄰");
    assert_eq!(get_status_icon_optimized(FileStatus::Downloading), "󰘨");
    assert_eq!(get_status_icon_optimized(FileStatus::Pending), "󰅍");
    assert_eq!(get_status_icon_optimized(FileStatus::Error), "󰈛");
}
*/
